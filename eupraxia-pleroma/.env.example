# Copy this file to .env and fill in your provider API keys if you want to enable real model usage
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
# Optional: set priority (comma-separated)
PROVIDER_PRIORITY=openai,anthropic,google
# DeepSeek API key (server-side)
DEEPSEEK_API_KEY=
# Blackbox API key (server-side)
BLACKBOX_API_KEY=
# Hugging Face API token (store one token with proper scopes). You may have multiple tokens; prefer a server-side secret with read/write as needed.
HF_API_TOKEN=
# Default provider used by server proxy (deepseek, blackbox, openai, anthropic, google)
DEFAULT_PROVIDER=deepseek
# Enable a local transformers model (not recommended on Windows unless you know what you're doing)
USE_REAL_MODEL=0
MODEL_NAME=gpt2

# ---------------------------------------------------------------------------
# Self-evolution / automation variables
# These control lightweight evolution cycles and should be configured on the
# server (do not commit secrets). Use a secrets manager in production.
# ---------------------------------------------------------------------------
# API key clients use to call the server proxy endpoints (never expose provider
# tokens to the frontend). Set this to a random secret and keep it server-side.
PROXY_API_KEY=

# Base URL used by local evolution scripts to call the API (default localhost)
# If your server runs on a different host: set BASE_URL accordingly.
BASE_URL=http://127.0.0.1:8000

# How many synthetic examples to generate per run (default 200)
SYNTH_EXAMPLES=200

# If you want the backend to perform real training cycles, enable these (advanced)
# Note: heavy training requires pinned deps (peft, bitsandbytes, torch) and
# usually a Linux host or container. Keep these empty unless you plan to enable.
ENABLE_FINE_TUNE=0
# Path or name for the base model used when fine-tuning (server-side only)
BASE_MODEL=Kuaishou/KAT-Dev-72B-Exp

# ---------------------------------------------------------------------------
# SECURITY NOTE: Never paste API tokens into chat or commit them to source.
# If any tokens were shared publicly, revoke them and create new tokens.
# Store secrets in environment variables, a cloud secret manager or an
# encrypted file accessible only to the server process.
# ---------------------------------------------------------------------------
